name: food_safe
description: A food safety management app with user profile photo feature.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  shared_preferences: ^2.2.0
  flutter_image_compress: ^2.0.3
  path_provider: ^2.0.15
  path: ^1.8.3
  image_picker: ^1.0.1

  # Platform specific implementations
  shared_preferences_web: ^2.2.1
  path_provider_platform_interface: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  mockito: ^5.4.2
  build_runner: ^2.4.6

flutter:
  uses-material-design: true